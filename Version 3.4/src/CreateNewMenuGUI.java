import javax.swing.*;
import java.awt.*;

public class CreateNewMenuGUI extends JFrame {
    public CreateNewMenuGUI() {
        setTitle("Lancaster's Kitchen");
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setResizable(false);
        setSize(400, 300);
        setLocationRelativeTo(null);
        setLayout(null);
        setVisible(true);
    }
}
